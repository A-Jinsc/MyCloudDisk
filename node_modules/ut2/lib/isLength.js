'use strict';

var native = require('./internals/native.js');

function isLength(value) {
    return typeof value === 'number' && value > -1 && value % 1 === 0 && value <= native.MAX_SAFE_INTEGER;
}

module.exports = isLength;
