/**
 * 生成一个首个字符串处理的函数。
 *
 * @private
 * @param {string} methodName 首个字符串的大小写方法。
 * @returns {string} 新的大小写函数。
 */
declare function createCaseFirst(methodName: 'toLowerCase' | 'toUpperCase'): (string: string) => string;
export default createCaseFirst;
